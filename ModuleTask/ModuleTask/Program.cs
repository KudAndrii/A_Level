using System;
using System.Text;

namespace ModuleTask
{
    internal class Program
    {
        private static void Main(string[] args)
        {
            /*
            var logic = new AllLogic();

            int[] array = logic.GetIntArray();
            var oddNumbers = logic.GetOddOrEvenNumbers(array);
            var evenNumbers = logic.GetOddOrEvenNumbers(array, true);
            var firstCharArray = logic.ChangeNumbersOnLetters(oddNumbers);
            var secondCharArray = logic.ChangeNumbersOnLetters(evenNumbers);
            logic.SomeLettersToUpper(ref firstCharArray);
            logic.SomeLettersToUpper(ref secondCharArray);
            logic.CompareArraysByUpperLetters(firstCharArray, secondCharArray);
            Console.WriteLine("This is the first array: " + string.Join(' ', firstCharArray));
            Console.WriteLine("This is the second array: " + string.Join(' ', secondCharArray));
            */
        }
    }
}
